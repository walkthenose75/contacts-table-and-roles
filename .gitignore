# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code
.claude/settings.local.json

# Power Apps environment binding — generated per-environment by `power-apps init`
# and `add-data-source`. Never commit these; run `node setup.mjs` (see SETUP.md) to
# regenerate them for your own environment. A placeholder shape lives in
# power.config.template.json. (dataSourcesInfo.ts is generic and IS committed so the
# app builds before init.)
power.config.json
.power/schemas/**/*.Schema.json

# Local environment variables (your Dataverse org URL, etc.)
.env
.env.local
.env.*.local
