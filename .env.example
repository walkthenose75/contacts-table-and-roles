# Copy this file to `.env.local` and fill in your own values.
# `.env.local` is gitignored and never committed.

# Your Dataverse environment (org) URL, no trailing slash.
# Find it in Power Apps > Settings > Developer resources > Web API endpoint.
VITE_DATAVERSE_ORG_URL=https://YOUR_ORG.crm.dynamics.com
